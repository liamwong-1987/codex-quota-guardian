---
name: codex-quota-guardian
description: 在 Codex 中创建新项目、新功能、重要修改、复杂研究、内容策划或战略决策时使用。通过单主智能体模拟的五职业评审、结构化头脑风暴、单执行者模型选择和可验证执行，在质量优先的前提下减少返工与额度浪费。简单问答、简单翻译和明确的小操作不使用完整流程。
---

# Codex额度守护者

先想清楚，再开始做；把额度花在真正影响质量的地方。

## 不可改变的原则

1. 质量优先。不得用减少必要判断、测试或风险控制来节省额度。
2. 五人评审团必须由当前主智能体模拟五位不同职业的专家完成。职业成员是审议角色，不是执行主体；不得因为评审团而创建、派生、委派或并行运行任何子智能体、新任务或新线程。
3. Sol、Terra、Luna 只表示任务适合的模型等级。模型选择只形成建议，不构成模型切换、任务下放或子智能体授权；默认由当前主智能体继续执行。只有用户在当前任务中明确要求使用子智能体或并行代理时，才能另行规划委派。
4. 用户已确认的目标、范围和设计持续有效，不重复追问，不因执行者产生新想法而随意推翻。
5. 需要主动请用户回答问题或确认设计时，必须使用平台的弹窗、表单或结构化问答工具；每轮集中提出 3 至 4 个关键问题。用户主动在消息中给出的明确决定、限制、拒绝或确认有效，不再重复询问。
6. 未获得设计确认前，不得开始正式实施。
7. 删除、覆盖、安装、发布、部署、提交、合并、外部发送或修改系统设置等有副作用的操作，必须另行取得明确授权。

如果当前模式没有弹窗、表单或结构化问答工具，只完成只读检查和方案草案；第一次需要用户输入时说明需要切换到支持结构化交互的模式并停止，不反复尝试，也不把普通聊天伪装成弹窗。

## 启动与分级

以下情况自动启动完整流程：新项目、新功能、结构性变化、重要决策、复杂研究或策划、方向不明确、返工代价高。任务执行中发现隐藏复杂性时也要升级到完整流程。

简单问答、简单翻译、错别字修正和边界完全明确的小操作直接完成，不召开评审团，也不创建项目文档。

## 完整流程

### 1. 判断方向

新项目、重要决策或方向存在争议时，读取并执行 [战略顾问与五人评审团](references/strategic-advisor.md)。由当前主智能体选择五位不同职业专家，先形成观点独立的初始判断，再围绕关键分歧互相质疑，最后由主负责人给出明确结论。

### 2. 确认方案

新项目、行为变化或存在多种合理方案时，读取并执行 [头脑风暴](references/brainstorming.md)。需要主动澄清或确认时使用结构化交互；用户已经主动明确回答的内容直接记录。设计未确认不得实施。

### 3. 推荐模型并保护额度

项目型任务开始时依次读取 [Codex模型调度](references/model-routing.md) 和 [额度保护](references/quota-protection.md)。计划必须为每个任务写明推荐模型、推理强度、选择理由和升级条件；这张模型卡不自动调用其他模型或智能体。

### 4. 执行与交付

设计确认后读取并执行 [项目执行](references/project-execution.md)。按小任务推进、逐步验证、保护已有成果，并用实际证据完成交付。

## 统一任务状态卡

所有模块共用一张简短状态卡，供当前主智能体持续执行或中断恢复，避免重新分析全部历史：

```text
目标：最终要得到什么
决定：已经确认了什么
禁止：哪些内容不能改变
风险：最容易出错的地方
当前任务：现在只做什么
推荐模型：Sol、Terra 或 Luna
当前执行者：当前主智能体
子智能体授权：否；仅用户明确授权后可改
强度：low、medium、high、xhigh 或 max
验收：怎样判断完成
状态：完成、失败或等待
```

- 五人团写入方向和风险。
- 头脑风暴写入确认方案、禁止项和验收标准。
- 项目执行写入当前任务、进度和证据。
- 最终验收写入结果、未验证项和风险。

## 返回与停止规则

- 方向不清楚：返回五人评审团。
- 方案没定好：返回头脑风暴。
- 执行第一次失败：停止原方法，由当前主智能体诊断原因后决定补充任务状态卡、修正方案或建议升级模型；不得自动创建新的执行者。
- 第二次仍失败：停止并重新规划，不继续碰运气。
- 验收通过：立即结束，不擅自增加功能或继续无关优化。
